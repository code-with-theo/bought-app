# 静态 Web / Capacitor 构建时使用：Cloudflare Worker API 的公开 HTTPS 地址。
# 示例：https://bought-api.<account-subdomain>.workers.dev
NEXT_PUBLIC_API_BASE_URL=

# Worker 运行时变量写在 worker/.dev.vars（仅本地，已忽略）或 Cloudflare Dashboard。
# 需要：ALLOWED_ORIGINS=https://bought-app.<account-subdomain>.workers.dev,capacitor://localhost
# 不要在此文件或仓库中写任何会话、数据库或账户密钥。
